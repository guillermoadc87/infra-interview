apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
spec:
  template:
    spec:
      containers:
        - name: inventory-service
          env:
            - name: ENVIRONMENT
              value: preview
            - name: LOG_LEVEL
              value: debug
            - name: LOW_STOCK_THRESHOLD
              value: "10"
