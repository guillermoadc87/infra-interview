apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# DYNAMIC per-application credentials from Vault's database secrets engine.
# Used by the long-lived environments.
resources:
  - resources.yaml
