# CONFIG CATEGORY 3 — environment identity. See variants/env/dev/env-identity.yaml.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-service
spec:
  template:
    spec:
      containers:
        - name: api-service
          env:
            - name: ENVIRONMENT
              value: staging
