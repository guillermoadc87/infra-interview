apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base
- pdb.yaml

components:
- ../../variants/tier/prod
- ../../variants/env/prod

patches:
- path: replicas.yaml
- path: resources.yaml
- path: settings.yaml

# CATEGORY 1. Note: prod carries NO Image Updater annotations (see the apps
# ApplicationSet templatePatch), so this value only ever changes via the gated
# promotion workflow.
images:
- name: ghcr.io/guillermoadc87/api-service
  newTag: prod-20260817T143909Z-15bdb10
