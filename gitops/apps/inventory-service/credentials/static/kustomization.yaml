apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# STATIC credential from Vault's KV store. Used by PR previews, which run their
# own throwaway Postgres that Vault has no connection to.
resources:
  - external-secret.yaml
