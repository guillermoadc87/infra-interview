# CONFIG CATEGORY 3 — environment identity. See variants/env/dev/env-identity.yaml.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
spec:
  template:
    spec:
      containers:
        - name: inventory-service
          env:
            - name: ENVIRONMENT
              value: prod
